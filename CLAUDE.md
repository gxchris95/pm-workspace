# Claude Code Instructions

Read `AGENTS.md` and follow it as your operating manual for this workspace.

Skills are in `skills/`. When the user invokes a skill by name (e.g., `/intake`, `/prd`), read the corresponding skill file and follow its process. See `skills/SCHEMA.md` for the full catalog.
