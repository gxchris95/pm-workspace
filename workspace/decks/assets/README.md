# Deck assets

Logos, screenshots, diagrams referenced by decks.

Use relative paths from the deck file:

```markdown
![Logo](../assets/logo.png)
```

Keep file sizes reasonable — large images bloat the rendered PPTX.
